# Sample Repo Fixture

Place a small, real (or realistic) git repo here for integration tests —
per docs/prism-v1-build-spec.md Section 9, step 9 ("Testing against real repos").

It should have enough commit history and a few cross-file function calls
to exercise both `prism why` and `prism impact` meaningfully.
